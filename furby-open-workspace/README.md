# Furby Open Workspace

This is the assistant's private, user-facing file workspace.

Telegram uploads, generated files, notes, and other runtime content may be stored here. Everything in this folder is ignored by Git except this README, so private content is not included in source commits.

Back up this directory separately if it contains files you care about.
